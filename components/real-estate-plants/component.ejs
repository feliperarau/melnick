<% 
// Params
var args = typeof args !== 'undefined' ? args : '';
let cssClass = args.cssClass ?? '';
%>

<div class="_real-estate-plants <%= cssClass %>">
    <div class="top-section">
        <h2 class="features-title title-display">Plantas</h2>
        <div class="actions">
            <%- 
            include( '../button-default/component.ejs', { args: {
                icon: 'plant',
                label: 'Unidades',
            } } );
            %>
            <%-
            include( '../button-default/component.ejs', { args: {
                icon: 'plant',
                label: 'Infraestrutura',
                variation: 'outline',
            } } );
            %>
            <%-
            include( '../button-default/component.ejs', { args: {
                icon: 'plant',
                label: 'Áreas comuns',
                variation: 'outline',
            } } );
            %>
        </div>
    </div>
    <div class="bottom-section">
        <div class="bottom-inner">
            <div class="swiper">
                <div class="swiper-wrapper">
                    <div class="swiper-slide">
                        <span class="slide-label">Unidade 241m²</span>
                        <img class="image" src="../../assets/img/real-estate-inner/plant-1.png" />
                    </div>
                    <div class="swiper-slide">
                        <span class="slide-label">Unidade 300m²</span>
                        <img class="image" src="../../assets/img/real-estate-inner/plant-1.png" />
                    </div>
                </div>
                <%- 
                include( '../button-icon/component.ejs', { args: {
                    icon: 'fullscreen',
                    cssClass: 'fullscreen'
                } } );
                %>
                <div class="swiper-nav-container">
                    <button type="button" class="next-slide">
                        <i class="icon icon-chevron-right-2"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>