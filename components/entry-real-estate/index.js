const entryRealEstate = () => {
  const ref = document.querySelectorAll("._entry-real-estate");

  if (!ref.length) return;

  // For every component on the page...
  for (let c = 0; c < ref.length; c++) {
    const component = ref[c];
  }
};

entryRealEstate();

export default entryRealEstate;
